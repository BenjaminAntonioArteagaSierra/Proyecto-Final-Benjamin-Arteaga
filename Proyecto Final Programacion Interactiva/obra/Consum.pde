class Consum {
  
  void Dibujar () {
    stroke(s);
    fill(in);
    rect (0,80,50,200);//x, y, width, hight
    
    stroke(s);
    fill(in);
    rect (50,200,130,80);//x, y, width, hight
  }
}
